<?xml version="1.0" encoding="utf-8"?>
<!--
  Warehouse Wizard — REFERENCE ONLY.

  This is what the final, merged AndroidManifest.xml looks like after
  Bubblewrap generates its project from twa-manifest.json and CI splices in
  manifest-fragments/permissions.xml + manifest-fragments/application-entries.xml.

  CI does NOT copy this file over the generated one (that would discard
  Bubblewrap's own required TWA metadata, which changes between Bubblewrap
  versions). This file exists so a human can see the intended end state,
  diff against it, or merge by hand for a fully manual build. The two
  fragment files are the actual source of truth for automation.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.warehouse.wizard">

    <!-- from manifest-fragments/permissions.xml -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bubblewrap default -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">

        <!-- Bubblewrap default: the TWA launcher activity -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:label="@string/launcherName"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Toolbar">
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://warehousewizard.app/?source=twa" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="warehousewizard.app" />
            </intent-filter>
        </activity>

        <!-- Bubblewrap default: standalone-mode delegate -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.FocusActivity"
            android:exported="false" />
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="false" />

        <!-- from manifest-fragments/application-entries.xml -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DownloadTriggerReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.warehouse.wizard.ACTION_DOWNLOAD_UPDATE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DownloadCompleteReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
